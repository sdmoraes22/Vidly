@model Vidly.ViewModels.MoviesFormViewModel
@{
    ViewBag.Title = "MoviesForm";
    Layout = "~/Views/_ViewStart.cshtml";
}

@if (Model.Movie == null)
{
    <h1>New Movie</h1>
}
else
{
    <h1>Edit</h1>
}

@using (Html.BeginForm("Save", "Movies"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.Name)
        @Html.TextBoxFor(m => m.Movie.Name, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Movie.GenreId)
        @Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.Genres, "Id", "Name"), "Genre", new { @class = "form-control" })
    </div>

    @Html.HiddenFor(m => m.Movie.Movieid)
    <button type="submit" class="btn btn-primary">Save</button>
}


